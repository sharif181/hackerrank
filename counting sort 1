#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,i;
    cin>>n;
    int ar[n];
    int br[n]={};
    for(i = 0;i<n;i++)
        cin>>ar[i];
    
    for(i=0;i<n;i++)
    {
        br[ar[i]]++;
    }
    for(i = 0;i<n;i++)
        cout<<br[i]<<" ";

    return 0;
}
