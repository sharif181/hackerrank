#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,i,a;
    vector<int> br(100);
    cin>>n;
    for(i = 0;i<n;i++){
        cin>>a;
        br[a]=br[a]+1;
    }
    for(i = 0;i<100;i++)
        cout<<br[i]<<" ";

    return 0;
}
